import React from "react";
import ReactDOM from "react-dom/client";
import Cubetimer from "./Cubetimer.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Cubetimer />
  </React.StrictMode>
);
